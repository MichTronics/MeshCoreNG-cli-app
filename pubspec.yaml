name: meshcli_ng
description: Professional Android mesh management console for MeshCoreNG.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  flutter_reactive_ble: ^5.4.0
  usb_serial: ^0.5.2
  intl: ^0.19.0
  collection: ^1.18.0
  flutter_libserialport: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
